[project]
name = "distracted-driver-detection-challenge"
version = "0.1.0"
description = "A deep learning pipeline for detecting driver distraction."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
